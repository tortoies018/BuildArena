# S01 稳定轨道：接续学习文件

> 本文件是 S01 的单一接续入口。任何新 Agent、新绘画机器或新工作环境必须先完整阅读本文件；不需要依赖旧聊天即可继续。最后更新：2026-09-05，当前候选为 V4。

## 1. 用户要求

- 设计 Construction Challenge 第一赛道 S01 “Stable Orbital / 稳定轨道”的飞船。
- 可以采用较复杂结构，但必须优先保证方向正确、稳定入轨、三轴可控和结构完整。
- 控制限定为方向键与数字小键盘：`↑/↓` 启停主推，`←/→` 偏航，小键盘 `8/2` 俯仰，小键盘 `4/6` 侧倾。
- 工具坐标 `+Z` 是游戏竖直上方，`+Y` 是水平轴。
- 正常火箭必须鼻锥、主油箱在上，发动机、喷口和支脚在下；点火时喷焰朝下，火箭向上。
- 先建赛道目录，所有模型与配套文件直接平铺在赛道目录中，不再套模型版本文件夹。
- 文件名顺序必须为“版本号—名称—时间”；说明词放在名称后、时间前，时间永远位于基本名最后。
- 每次工作完成后更新本文件，使下一位 Agent 能独立继续。

## 2. 归档结构

唯一正式赛道目录：`level_designs/S01-稳定轨道/`

目录内不允许模型子目录。命名模板：

```text
V版本号-模型名称-YYMMDD-HHMMSS.bsg
V版本号-模型名称原始结构-YYMMDD-HHMMSS.bsg
V版本号-模型名称构建记录-YYMMDD-HHMMSS.json
V版本号-模型名称完整记录-YYMMDD-HHMMSS.json
V版本号-模型名称使用说明-YYMMDD-HHMMSS.md
V版本号-模型名称学习摘要-YYMMDD-HHMMSS.md
```

主 `.bsg` 是写入合法控制 `Data` 的测试文件；“原始结构”是 MCP 封存且控制为空的证明副本。禁止把 `_tuned` 放到时间之后。模型 XML 内部 `Machine name` 应与文件基本名一致。

封面只能使用 Besiege 实际载入并保存同一模型后产生的真实缩略图；没有真实缩略图时留空，不能用概念图、旧版本截图或占位图替代。

## 3. 最重要的方向知识

必须区分四件事：建造轴、发动机位置、喷焰方向、火箭受力方向。

1. 工具建造轴：`+Z` 为竖直上方。V1 已通过截图证明 `+Y` 不是竖直轴。
2. 发动机位置：Booster 必须物理位于主油箱和鼻锥下方。V2 虽竖直，但发动机位于顶端，仍然上下颠倒。
3. 喷焰方向：点火后火焰和烟雾必须朝地面向下。
4. 受力方向：火箭的反作用力与喷焰相反；喷焰向下，火箭才应向上。

V3 提供了关键反例：Booster 装在小油箱顶面时，MCP 返回 `Thrust ... straight up`，但实机截图显示喷焰也向上，火箭实际受到向下的反作用力。因此 MCP 的 Booster 朝向文字不能单独作为真实推进验收。今后必须同时记录工具文字、完整几何位置和实机短点火；三者冲突时，以实机喷焰及真实运动为准。

## 4. 版本历史

### V1 轨道飞镖：横轴错误，已淘汰

- 文件：`V1-轨道飞镖-260904-210409.bsg`。
- 17 块；沿工具 `+Y` 建造。
- 实机显示整船横躺。
- 结论：竖直火箭必须沿工具 `+Z`。

### V2 轨道长矛：发动机位于顶端，已淘汰

- 文件：`V2-轨道长矛-260904-212909.bsg`。
- 39 块；双大油箱、四发、四支脚、六反作用轮、四 RCS、12 Brace。
- 已沿 `+Z` 竖直，但四台 Booster 装在上部大油箱顶面。
- 实机截图确认上下关系错误。
- 结论：竖直轴正确不代表火箭上下正确。

### V3 底置四发轨道火箭：喷焰向上，已淘汰

- 文件：`V3-底置四发轨道火箭-260905-124510.bsg`。
- 47 块；Booster 位于底部四个外伸小油箱的顶面。
- MCP 返回 Booster 方向 `straight up`，先前被误解释为有效推力向上。
- 用户实机截图显示四股喷焰向上；反作用力实际向下。
- 结论：Booster 应装到燃料舱底面，让喷口和火焰向下。V3 只作为第三个失败样本。

### V4 下喷式四发轨道火箭：当前候选

- 主文件：`V4-下喷式四发轨道火箭-260905-131830.bsg`。
- 原始结构：`V4-下喷式四发轨道火箭原始结构-260905-131830.bsg`。
- MCP 生命周期：`V4_Downward_Exhaust_Orbital_Rocket_260905_131830`。
- 状态：结构和控制静态验收通过；已选定一份 526.73 秒 Tracker 轨迹和一份 8 分 21 秒录屏作为提交候选，但尚未用官方评分器确认圈数与成绩。

## 5. V4 结构

V4 共 51 块：

| 部件 | 数量 | 用途 |
| --- | ---: | --- |
| Starting Block | 1 | 跟踪和受力根节点 |
| Fuel Barrel Big | 2 | 中央箭体与 200 单位主燃料 |
| Double Wooden Block | 4 | 东西南北底部支臂 |
| Single Wooden Block | 5 | 四个发动机舱升高支座，加一个顶部鼻杆 |
| Fuel Barrel | 4 | 底部四周独立供油舱 |
| Booster | 4 | 吊装在小油箱底面，预期喷焰朝下 |
| Grip Pad | 4 | 四点落地并保护发动机间隙 |
| Reaction Steering Block | 6 | 三个正交轴各一对 |
| Steering Thruster | 4 | 上部 RCS 细调 |
| Fuel Line | 4 | 四个小油箱分别接入下部大油箱 |
| Brace | 12 | 根部 4、级间 4、发动机舱斜撑 4 |
| Armor Plate Round | 1 | 圆形鼻头，明确顶部 |

工具坐标关键位置：

- Starting Block `[0,0,0]`。
- 大油箱 `[0,0,2]` 与 `[0,0,5]`。
- 四个升高支座中心位于 `[±2,0,1]`、`[0,±2,1]`。
- 四个小油箱位于 `[±3,0,1]`、`[0,±3,1]`。
- 四台 Booster 吊在小油箱底面，中心位于 `[±3,0,0]`、`[0,±3,0]`；MCP 返回 `straight down`，在本版应理解为喷口/喷焰朝下。
- Grip Pad 中心 `z=-0.75`，低于发动机本体，提供落地间隙。
- 鼻杆和鼻头位于 `z=7`、`z=7.88`，为最高部件。

注意：这些是 MCP 工具坐标。BSG XML 会把工具竖直轴写入不同的 Transform 字段，不能只根据 XML 的 `x/y/z` 字段名重新推断方向。

## 6. V4 控制

主文件仅修改原始结构副本中的合法 `Data`，几何和连接不得改变。

| 按键 | 控制部件 |
| --- | --- |
| `↑ / ↓` | 四台 Booster 同步启动 / 关闭；toggle 模式，非自动 |
| `← / →` | 两个竖直轴反作用轮控制偏航 |
| 小键盘 `8 / 2` | 东西侧反作用轮与 RCS 控制俯仰 |
| 小键盘 `4 / 6` | 南北侧反作用轮与 RCS 控制侧倾 |

相反安装面的控制块交换 `bmt-left` / `bmt-right`。反作用轮统一启用自动制动、`speed=2.0`、`damper=1.5`；RCS 启用自动制动。若实机只有某个姿态轴反向，只交换该轴整对键值，不增加几何版本。

## 7. V4 静态验收结果

- 原始结构与主文件均能作为 XML 解析。
- 两版均为 51 块。
- GUID、块类型和全部 Transform 差异为 0。
- 只有 14 个控制块的 `Data` 改变：Booster 4、Reaction Steering Block 6、Steering Thruster 4。
- 四台 Booster 均写入 `UpArrow` 启动、`DownArrow` 关闭。
- 有效记录 53 项：`start 1 + attach 34 + connect 16 + twist 2`，生成 51 块。
- 完整记录 54 项，并包含封存前的最终 `get_machine_summary`。
- 最终摘要确认四台 Booster 位于燃料舱下方、方向为 `straight down`，Grip Pad 低于发动机本体。

## 8. 游戏内验收流程

静态 XML 不能替代以下测试：

1. 加载 V4 主文件，确认鼻头在上、四台 Booster 吊在小燃料舱下方、四个 Grip Pad 接地且发动机不触地。
2. 不点火，短按 `←/→`、小键盘 `8/2`、`4/6`，确认三轴响应与自动制动。
3. 极短按一次 `↑`，必须看到四股喷焰同步朝下；立即按 `↓`。
4. 只有确认火箭向上离地，才做第二次较长点火。若仍向下，保存截图并创建 V5，不要仅改按键。
5. 检查四路 Fuel Line 持续供油；最大推力、急转和落地后不得连锁解体。
6. 用小键盘 `8/2` 转入切向加速，方向键修正偏航，小键盘 `4/6` 消除侧倾。
7. 先完成连续三圈，再缩短有效飞行时间；保存 Tracker 轨迹 CSV。
8. 结束时确认全部 51 个原始块仍连接 Starting Block，并在 Besiege 中保存真实缩略图。

## 9. 新 Agent 接续规则

1. 先读本文件和 V4 使用说明，首选测试 V4；V1、V2、V3 不再作为候选。
2. 只改合法按键、力度或制动参数时保留 V4 版本号，并同步更新说明与本文件。
3. 只要需要移动、翻转、增加、删除或重新连接方块，就保留 V4 并新建 V5 MCP 生命周期。
4. 新火箭仍沿工具 `+Z`，但发动机验收以“喷焰朝下、火箭向上”的实机结果为最终标准。
5. 新文件直接放入 `level_designs/S01-稳定轨道/`，不得建立模型子目录；文件名必须版本开头、时间结尾。
6. 原始结构封存前必须调用 `get_machine_summary`，再保存并关闭生命周期。
7. 完成后更新版本历史、当前候选、静态结果、动态结果和文件索引，保证下一位 Agent 仅凭本文件即可继续。

## 10. 文件与同步状态

- V1：模型及记录，坐标错误样本。
- V2：主版、原始结构、记录与说明，顶部发动机错误样本。
- V3：主版、原始结构、记录与说明，喷焰向上错误样本。
- V4：主版、原始结构、两份记录、使用说明与学习摘要，当前候选。
- V4 已选轨迹：`V4-下喷式四发轨道火箭轨迹-260905-140013.csv`；10 Hz、5268 个采样、约 526.73 秒。该文件已被用户选作提交候选，但尚未使用官方评分器确认圈数与成绩。
- V4 对话归档：`V4-下喷式四发轨道火箭对话记录-260905-131830.md`。
- 本文件同时镜像到 `model_learning/S01-稳定轨道-总学习文件.md`。

工作区与 Besiege 均使用平铺的 `S01-稳定轨道` 目录。Besiege 根目录中由 MCP 自动生成的生命周期目录只是历史备份，不属于正式赛道归档；未经用户明确授权不要永久删除。

## 11. 当前提交包

- 整理目录：`submissions/V4-下喷式四发轨道火箭提交包-260905-142303/`。
- 官方六文件 ZIP：`submissions/V4-下喷式四发轨道火箭官方六文件-260905-142303.zip`。
- ZIP 只含 `machine_raw.bsg`、`build_history.json`、`build_history_full.json`、`machine_tuned.bsg`、`trajectory.csv`、`chat_transcript.md`。
- ZIP SHA-256：`fb4b6c4ee4852f55ab729397fedcdd95e2671242d9e208ded63b1a990492f853`。2026-09-05 在对话记录加入最终提交阶段后重新生成，仍恰好包含六项。
- 整理目录另含 `提交说明.md`、`SHA256SUMS.txt`、已完成的 `kaggle_writeup.md` 和补充录像 `flight_video.mp4`。
- 录像为 543,618,999 字节（约 518.44 MiB）、8 分 21 秒，SHA-256 为 `855625b875ed668d3482d69365ab1553f80179e98ae2e237656a27f6ae990317`。
- 公开视频地址：`https://github.com/tortoies018/BuildArena/blob/main/submissions/V4-%E4%B8%8B%E5%96%B7%E5%BC%8F%E5%9B%9B%E5%8F%91%E8%BD%A8%E9%81%93%E7%81%AB%E7%AE%AD%E6%8F%90%E4%BA%A4%E5%8C%85-260905-142303/flight_video.mp4`。
- 录像、Writeup、提交说明和校验表属于补充材料，不得加入“官方六文件”ZIP；六文件 ZIP 始终保持恰好六项。
- 推荐申报 `Build with Agent / Copilot`：所有提交几何均由 MCP 生成且未手改，但完整迭代过程中使用了用户截图和纠错反馈。

## 12. Git 仓库状态

- 项目根目录 `E:/Desktop/BuildArena` 已初始化为 Git 仓库，当前分支为 `main`，跟踪 `origin/main`。
- GitHub 远程为 `https://github.com/tortoies018/BuildArena.git`。
- 首个提交为 `8ae0bf8606373aa2ef5e17645320a0ff6ec6dec7`；已成功推送，并验证当时的本地 HEAD 与远端 `main` 完全一致。
- 后续 Agent 不得擅自改远程地址或向其他分支推送；只有在用户授权的工作范围内提交和同步。
- 根目录 `.gitignore` 忽略：嵌套的 `BuildArena-2.0` 上游仓库、`.staging`/`staging` 临时目录、本地 `.env` 与缓存、可重新生成的压缩包以及一般原始屏幕录像；只对选定的 `submissions/.../flight_video.mp4` 设置精确例外。
- `.gitattributes` 只把选定的 `flight_video.mp4` 交给 Git LFS，不把所有 MP4 自动纳入版本控制。原因是该录像超过 GitHub 普通文件 100 MiB 上限；GitHub Free 的 LFS 单文件上限为 2 GB，当前录像在范围内。
- 比赛核心文件必须保持可跟踪：`.bsg`、构建 `.json`、轨迹 `.csv`、说明/学习 `.md` 和真实模型封面图片均未被忽略。
- `submissions` 中的解压后六个官方文件、最终 Writeup、提交说明、校验表与 LFS 录像可进入版本控制；对应 ZIP 因可重新生成而被忽略。

## 13. 录屏、Writeup 与远程提交接续信息

- 用户原始录像仍保留在项目根目录 `屏幕录制.mp4`，没有移动或覆盖；该原始副本继续被 `.gitignore` 忽略。
- 正式补充录像位于 `submissions/V4-下喷式四发轨道火箭提交包-260905-142303/flight_video.mp4`，以后不要用普通 Git 解除 LFS 跟踪，也不要在不必要时重复上传新版本；GitHub LFS 对每个新二进制版本都会重新计算存储。
- `kaggle_writeup.md` 已写成可直接提交的英文版，包含 Track、Run Summary、Video、LLM / Agent Setup、Prompts and Workflow、Controls、Code and Tools、Iteration Notes 和 Submission Artifacts。
- Writeup 明确不虚构未验证的完成圈数或官方得分。Kaggle 正式评测完成后，应把排行榜成绩和圈数追加到 Writeup、提交说明及本学习文件，不要回写猜测值。
- 视频的 Kaggle URL 使用上面的 GitHub `blob/main/.../flight_video.mp4` 公共页面。若 GitHub 页面要求下载才能播放，仍是公开原文件地址；可在 Kaggle 文字中保留相同链接。
- 处理这个 518 MiB 文件需要安装 Git LFS。继续工作前可运行 `git lfs ls-files` 确认录像显示为 LFS 对象，再用 `git status --short --branch` 和 `git ls-remote origin refs/heads/main` 检查同步状态。
- 本次远程发布完成后应把“已推送、公开视频可访问”的结果写回本节；若上传因 LFS 额度或网络失败，不得把链接描述为已发布。
